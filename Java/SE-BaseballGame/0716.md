# ⚾ BaseBallGame review

```java
public interface BaseBallResult {
	intMIN_COUNT= 0;
	intMAX_COUNT= 3;

	void checkInput(char inputChar, String randomValue, int count);
	int getResult();
	/*public abstract*/ void method(); //  /**/내 생략된 표현
}
```

interface 사용

# 🎰 LottoSimulator review

- [ ]  List로 변경하면서 생긴 오류 수정
    - (실행하면 오류)
- [ ]  Output 정리
